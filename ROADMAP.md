# ROADMAP

מה שנשאר מחוץ לגרסה הראשונה, בכוונה. הרשימה כאן כדי שהחלטות לא יישכחו, לא כדי
שהכול ייבנה. הכלל: פיצ'ר נכנס רק אם הוא לא מסבך את המסך הראשי.

---

## 1. מתכונים ותכנון ארוחות

הוספת כל מצרכי מתכון לרשימה בלחיצה אחת.

- טבלאות `recipes` + `recipe_items`, שתיהן `household_id not null`.
- מצרך במתכון מצביע ל-`catalog_items` ולא לטקסט חופשי, אחרת המיזוג לפי
  `name_norm` יישבר.
- ההוספה עוברת דרך `addOne` הקיים, כדי שמיזוג הכמויות יעבוד כמו בהוספה ידנית.

**למה לא עכשיו:** דורש עורך מתכונים, וזו אפליקציה אחרת.

## 2. מזווה — מה יש בבית

- `pantry_items` עם `quantity` ו-`expires_at`.
- סגירת קנייה מזרימה את הפריטים למזווה במקום רק ל-`purchase_history`.
- התראה שבועית על מה שנגמר.

**למה לא עכשיו:** מחייב שהמשתמש יעדכן מה נגמר. בלי זה הנתונים משקרים תוך שבוע.

## 3. סריקת ברקוד

- `BarcodeDetector` בדפדפנים שתומכים, עם fallback להקלדה.
- טבלת מיפוי `barcode → catalog_item_id`, שנבנית מהמשפחות עצמן.
- Progressive enhancement מלא: iOS Safari לא תומך, והמסך חייב לעבוד בלעדיו.

**למה לא עכשיו:** בלי מסד ברקודים ישראלי הסריקה מחזירה «לא נמצא» ברוב הפעמים.

## 4. מחירים ותקציב

- `price_observations` (פריט, חנות, מחיר, תאריך), מוזן ידנית.
- סיכום «כמה הוציאנו החודש» על בסיס `purchase_history`.

**למה לא עכשיו:** הזנה ידנית של מחירים נשחקת מהר, והנתון הופך למטעה.

## 5. השוואת מחירים בין רשתות

- הזנת המחירים הפומביים לפי חוק שקיפות המחירים.

**למה לא עכשיו:** מאמץ תשתית גדול (ETL יומי), ולא באמת משנה את חוויית הרשימה.

---

## חוב טכני מוכר

- **`components/ui` מוחרג מ-`pnpm check:rtl`.** shadcn אותחל עם `--rtl`, ולכן
  הפלט כבר לוגי; מה שנשאר פיזי שם הוא סמנטי (`data-[side=left]:left-0`).
  קומפוננטה חדשה שמותקנת משם צריכה בדיקה ידנית.
- **עריכת פריט שעוד לא סונכרן.** פריט שנוסף באוף-ליין אין לו עדיין id מהשרת,
  ולכן סימון או עריכה שלו נחסמים בהודעה. הפתרון הנכון הוא מיפוי
  `optimisticId → serverId` אחרי הסינכרון.
- **סגירת קנייה חסומה כשיש תור ממתין.** נכון והכי בטוח, אבל אפשר לשפר: לסגור
  אוטומטית אחרי שהתור מתרוקן.
- **`createFirstHousehold` לא אטומי.** בדיקת «אין אף משק בית» היא בקוד ולא ב-SQL,
  ולכן שתי בקשות שמגיעות בו-זמנית לדיפלוי טרי עלולות שתיהן לעבור אותה וליצור שני
  בתים. החלון הוא מילישניות, פעם אחת בחיי המערכת, והנזק הוא בית מיותר שאף אחד לא
  מחזיק את הקישור אליו. פתרון: אינדקס ייחודי חלקי או `create_household_if_first()`
  שמבצע את הבדיקה וההוספה בטרנזקציה אחת.
- **`bulkAdd` לא אידמפוטנטי בשרת.** הוא מתפרק לפריט-פריט רק במסלול האוף-ליין.
  אם נוסיף לו `clientId` דטרמיניסטי לכל chunk, המסלול יתאחד.
